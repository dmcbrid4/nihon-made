# Optional: leave empty to save study history in this browser.
DATABASE_URL=

# Required for database mode in production. Use a long, unique password.
# When set, the app uses HTTP Basic authentication (username: nihon).
APP_PASSWORD=
