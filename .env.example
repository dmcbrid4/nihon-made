# Leave all values empty for browser storage during local development.
# Cloud mode requires all four. Production fails closed without cloud setup.
DATABASE_URL=
SUPABASE_URL=
SUPABASE_PUBLISHABLE_KEY=
OWNER_EMAIL=
